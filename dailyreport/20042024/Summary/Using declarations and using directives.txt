Tổng hợp:

Để giảm bớt việc viêt đầy đủ không gian tên mỗi lần truy cập vào nó có thể dùng using declarations và using directives.
1. using declarations: 
	thông thường std::cout<<""<<endl;
	sử dụng using: using std::cout;
		       cout << ""<<endl;
2.Using-directives
	using namespace std;
	cout << "" << endl;